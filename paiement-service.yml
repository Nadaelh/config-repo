server:
  port: 8083

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/paiementdb
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver

spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
